export type LineRouteSection = {
  destination: string;
  direction: string;
  fromStation: string;
  routeId: number;
  serviceType: string;
  toStation: string;
  vehicleDestinationText: string;
};
